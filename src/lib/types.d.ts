export interface Localization {
  country: string;
  region: string;
  city: string;
  specificPlace: string;
  version: string;
  lat: number;
  lng: number;
  imageUrl: string;
  displayName: string;
}
export type ImageObject = {
  name: string;
  url: string;
};
